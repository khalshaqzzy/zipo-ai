/**
 * PostCSS configuration file.
 * This file configures the PostCSS plugins used in the build process.
 */
export default {
  plugins: {
    // Integrates Tailwind CSS into the PostCSS pipeline.
    tailwindcss: {},
    // Adds vendor prefixes to CSS rules for better browser compatibility.
    autoprefixer: {},
  },
};
